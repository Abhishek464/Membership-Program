server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/membershipdb
    username: abhishekgupta
    password:

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: never

springdoc:
  swagger-ui:
    path: /swagger-ui.html
